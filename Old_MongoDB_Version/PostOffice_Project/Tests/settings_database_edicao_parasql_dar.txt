DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'bdII_14366',   # nome da tua BD no pgAdmin4
        'USER': 'postgres',     # o teu user
        'PASSWORD': 'ir4119jo',
        'HOST': 'localhost',
        'PORT': '5432',
	 'TEST': {
            'MIRROR': 'default',  # <- this makes Django tests use the actual DB
        }
    }
}